include(CMakeFindDependencyMacro)

if("${QT_MAJOR_VER}" STREQUAL "6")
    find_dependency(Qt6 COMPONENTS Core Network REQUIRED)
else()
    find_dependency(Qt5 COMPONENTS Core Network REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/remotecommonTargets.cmake")
